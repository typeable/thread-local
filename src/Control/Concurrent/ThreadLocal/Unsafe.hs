module Control.Concurrent.ThreadLocal.Unsafe where

--import Control.Concurrent.ThreadLocal.Internal
--import Data.Map (Map)
--import qualified Data.Map (Map)


--dumpThreadLocal :: ThreadLocal a -> IO (Map ThreadId a)
--dumpThreadLocal (ThreadLocal ref) = do m <- 
